version: '3.8'
services:
  redis-db:
    image: redis:latest
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 20 1 --loglevel warning --requirepass admin
    volumes:
      - redis:/data
volumes:
  redis:
    driver: local